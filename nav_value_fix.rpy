init python:
    if not "rooms" in locals() | globals():
        rooms = []
    if not "locations" in locals() | globals():
        locations = []
    if not "maps" in locals() | globals():
        maps = {}
